<!-- 📄 File: app/views/layouts/application.html.erb -->
<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= content_for(:title) || "AfriXpress" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <!-- Bootstrap + Rails assets -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <%= javascript_importmap_tags %>

    <style>
      /* Ensure navbar and dropdowns always sit above sticky search bars */
      .navbar.sticky-top { z-index: 3000 !important; }
      .dropdown-menu { z-index: 3100 !important; }
      .navbar-collapse { text-align: left !important; }
    </style>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light sticky-top shadow-sm">
      <div class="container-fluid px-0">
        <a class="navbar-brand px-3" href="<%= root_path %>">AfriXpress</a>

        <!-- Cart always visible -->
        <div class="ms-auto me-3" id="cart-badge">
          <%= render "layouts/cart_badge" %>
        </div>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse px-3 text-start" id="navbarNav">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
              <%= link_to "Products", products_path, class: "nav-link" %>
            </li>
          </ul>

          <ul class="navbar-nav align-items-center gap-2">
            <!-- 🌍 Currency Selector -->
            <li class="nav-item dropdown">
              <button class="btn btn-sm btn-outline-secondary dropdown-toggle" type="button" data-bs-toggle="dropdown">
                Currency: <%= session[:currency] || "USD" %>
              </button>
              <ul class="dropdown-menu dropdown-menu-end">
                <li>
                  <%= button_to "KES", set_currency_path(currency: "KES"),
                      method: :post,
                      class: "dropdown-item btn btn-link p-0",
                      form: { "data-turbo" => "false" } %>
                </li>
                <li>
                  <%= button_to "USD", set_currency_path(currency: "USD"),
                      method: :post,
                      class: "dropdown-item btn btn-link p-0",
                      form: { "data-turbo" => "false" } %>
                </li>
              </ul>
            </li>

            <% if user_signed_in? %>
              <% unread_count = current_user.notifications.where(read: false).count %>

              <!-- My Account Dropdown -->
              <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="accountDropdown" role="button"
                   data-bs-toggle="dropdown" aria-expanded="false">
                  My Account
                </a>
                <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="accountDropdown">
                  <li><%= link_to "Profile", account_path, class: "dropdown-item" %></li>
                  <li><%= link_to "Notifications (#{unread_count})", notifications_path, class: "dropdown-item" %></li>

                  <% if current_user.buyer? %>
                    <li><%= link_to "Buyer Dashboard", buyer_dashboard_path, class: "dropdown-item" %></li>
                  <% end %>

                  <% if current_user.seller? %>
                    <li><%= link_to "Seller Dashboard", seller_dashboard_path, class: "dropdown-item" %></li>
                    <li><%= link_to "Storefront Settings", edit_seller_path(current_user.store_slug), class: "dropdown-item" %></li>
                    <li><%= link_to "My Shipments", shipments_path, class: "dropdown-item" %></li>
                  <% end %>

                  <% if current_user.admin? %>
                    <li><%= link_to "All Shipments", shipments_path, class: "dropdown-item" %></li>
                    <li><%= link_to "Admin Panel", admin_root_path, class: "dropdown-item" %></li>
                  <% end %>

                  <% if current_user.buyer? || current_user.seller? %>
                    <li><hr class="dropdown-divider"></li>
                    <li>
                      <%= button_to "Switch to #{current_user.active_role == 'buyer' ? 'Seller' : 'Buyer'} Mode",
                                    toggle_role_path,
                                    method: :post,
                                    class: "dropdown-item btn btn-link p-0",
                                    form: { "data-turbo" => "false" } %>
                    </li>
                  <% end %>

                  <li><hr class="dropdown-divider"></li>
                  <li>
                    <%= link_to "Logout", destroy_user_session_path,
                                data: { turbo_method: :delete },
                                class: "dropdown-item" %>
                  </li>
                </ul>
              </li>
            <% else %>
              <li class="nav-item"><%= link_to "Login", new_user_session_path, class: "nav-link" %></li>
              <li class="nav-item"><%= link_to "Sign Up", new_user_registration_path, class: "nav-link" %></li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>

    <!-- 🔔 Flash Messages -->
    <div id="flash-messages" class="px-3 pt-2">
      <% flash.each do |key, value| %>
        <div class="alert alert-<%= key == "notice" ? "success" : "danger" %> alert-dismissible fade show" role="alert">
          <%= value %>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
      <% end %>
    </div>

    <!-- 🔄 Page Content -->
    <div class="container-fluid px-0">
      <%= yield %>
    </div>
  </body>
</html>
