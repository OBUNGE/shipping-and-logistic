<div class="container my-5">
  <div class="card shadow-lg border-0 rounded-4">
    <div class="card-body p-4">
      <h1 class="fw-bold text-primary mb-4">Edit Discount</h1>

      <!-- Reuse the form partial -->
      <%= render "form", discount: @discount %>

      <div class="mt-3">
        <%= link_to "Back", discounts_path, class: "btn btn-outline-secondary rounded-pill" %>
      </div>
    </div>
  </div>
</div>
