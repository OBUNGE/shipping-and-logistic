<div class="container-fluid px-0">

<!-- 🦸 Hero Section -->
<section class="hero bg-dark text-white text-center py-5 mb-4">
  <div class="container">
    <h1 class="fw-bold display-5">
      Cheap Leather Bags & Men’s Shoes in Nairobi
    </h1>
    <p class="lead mb-4">
      Discover affordable leather products — handbags, men’s shoes, and accessories. 
      Shop quality leather in Kenya with secure M-PESA payments.
    </p>

    <% if (bags = Category.find_by(name: "Leather Bags")) %>
      <%= link_to "Shop Leather Bags", products_path(category: bags.slug),
          class: "btn btn-primary btn-lg shadow-sm me-2" %>
    <% end %>

    <% if (shoes = Category.find_by(name: "Men’s Leather Shoes")) %>
      <%= link_to "Shop Men’s Shoes", products_path(category: shoes.slug),
          class: "btn btn-outline-light btn-lg shadow-sm" %>
    <% end %>
  </div>
</section>


  <!-- 🧭 Sticky Category Bar -->
  <div class="category-bar sticky-top bg-white py-2 px-3 mb-3 d-flex align-items-center gap-3 overflow-auto border-bottom shadow-sm"
       style="top: 65px; z-index: 1050; white-space: nowrap;">
    <% Category.where(parent_id: nil).each do |category| %>
      <%= link_to products_path(category: category.slug),
          class: "category-pill px-3 py-2 rounded-pill text-dark fw-semibold text-decoration-none border hover-glow" do %>
        <%= category_icon(category.name) %>
        <span class="ms-2"><%= category.name %></span>
      <% end %>
    <% end %>
  </div>

  <!-- 🔍 Search Bar -->
  <div class="sticky-top bg-white shadow-sm py-2 px-3 mb-4" style="top: 115px; z-index: 1050;">
    <form action="<%= products_path %>" method="get" class="d-flex" role="search">
      <input type="text" name="query" class="form-control me-2 input-clean shadow-sm"
             placeholder="Search leather bags, men’s shoes, accessories..." value="<%= params[:query] %>">

      <button class="btn btn-primary px-4 btn-clean" aria-label="Search">
        <i class="bi bi-search"></i>
      </button>
    </form>
  </div>

  <div class="row">
    <!-- 📁 Sidebar -->
    <aside class="col-12 col-md-3 col-lg-2 mb-4 d-none d-md-block">
      <div class="accordion rounded-3 shadow-sm bg-white" id="categoryAccordion" style="top: 180px; position: sticky;">
        <% Category.where(parent_id: nil).includes(:subcategories).each_with_index do |category, idx| %>
          <div class="accordion-item border-0">
            <h2 class="accordion-header" id="heading-<%= idx %>">
              <button class="accordion-button collapsed fw-semibold small" type="button"
                      data-bs-toggle="collapse" data-bs-target="#collapse-<%= idx %>">
                <%= category_icon(category.name) %>
                <span class="ms-2"><%= category.name %></span>
              </button>
            </h2>
            <div id="collapse-<%= idx %>" class="accordion-collapse collapse">
              <div class="accordion-body p-2">
                <ul class="list-unstyled mb-0">
                  <% category.subcategories.each do |sub| %>
                    <li>
                      <%= link_to sub.name,
                          products_path(category: category.slug, subcategory: sub.slug),
                          class: "d-block py-1 ps-3 text-muted small link-hover" %>
                    </li>
                  <% end %>
                </ul>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    </aside>

    <!-- 🛍️ Product Grid -->
    <main class="col-12 col-md-9 col-lg-10">
      <div class="d-flex justify-content-between align-items-center mb-3">
        <h2 class="fw-bold text-dark fs-5 mb-0">
          🛍️ All Leather Products
        </h2>
        <% if user_signed_in? && current_user.seller? %>
          <%= link_to "+ Add Product", new_product_path, class: "btn btn-primary btn-sm btn-clean shadow-sm" %>
        <% end %>
      </div>

      <div class="row g-3">
        <% @products.each do |product| %>
          <% seller = product.seller %>
          <div class="col-6 col-md-4 col-lg-3">
            <div class="card h-100 border-0 shadow-sm rounded-4 hover-zoom position-relative">

              <!-- 🖼️ Image -->
              <div class="ratio ratio-1x1 bg-light position-relative">
                <%= link_to product_path(product) do %>
                  <% if product.image_url.present? %>
                    <%= image_tag product.image_url, alt: "#{product.title} | Cheap Leather Product Nairobi",
                                  class: "card-img-top rounded-top object-fit-cover" %>
                  <% else %>
                    <%= image_tag asset_path("placeholder.jpg"), alt: "Leather product placeholder",
                                  class: "card-img-top rounded-top object-fit-cover" %>
                  <% end %>
                <% end %>

                <% if product.discount.present? && product.discount.percentage.to_f > 0 %>
                  <span class="discount-badge position-absolute top-0 end-0 m-1">
                    -<%= product.discount.percentage.to_i %>% OFF
                  </span>
                <% end %>
              </div>

              <!-- 📦 Details -->
              <div class="card-body d-flex flex-column p-3">
                <h6 class="fw-semibold text-truncate mb-1">
                  <%= link_to product.title, product_path(product),
                      class: "text-dark text-decoration-none hover-link" %>
                </h6>

                <% if product.discount.present? && product.discount.percentage.to_f > 0 %>
                  <% discounted_price = product.price * (1 - product.discount.percentage / 100.0) %>
                  <p class="small mb-1">
                    <span class="text-muted text-decoration-line-through me-2">
                      <%= display_price(product.price) %>
                    </span>
                    <span class="text-danger fw-bold">
                      <%= display_price(discounted_price) %>
                    </span>
                  </p>
                <% else %>
                  <p class="text-primary fw-bold small mb-1">
                    <%= display_price(product.price) %>
                  </p>
                <% end %>

                <p class="small mb-1 <%= product.stock.to_i > 0 ? 'text-success' : 'text-danger' %>">
                  <%= product.stock.to_i > 0 ? "✅ In Stock" : "❌ Out of Stock" %>
                </p>

                <% if seller&.store_slug.present? %>
                  <p class="small text-muted mb-2">
                    <%= link_to (seller.company_name || seller.name),
                        seller_path(seller.store_slug),
                        class: "fw-semibold text-decoration-none hover-link" %>
                  </p>
                <% end %>

                <!-- 📞 Call + Add to Cart -->
                <div class="mt-auto d-flex gap-2">
                  <a href="tel:+254726565342" class="btn btn-clean btn-call flex-fill">
                    📞 Call
                  </a>

                  <% if product.stock.to_i > 0 %>
                    <%= button_to "Add", add_cart_path,
                        method: :post,
                        params: { product_id: product.id },
                        remote: true,
                        form: { data: { turbo_stream: true } },
                        class: "btn btn-clean btn-cart flex-fill" %>
                  <% else %>
                    <button class="btn btn-clean btn-secondary flex-fill" disabled>Out</button>
                  <% end %>
                </div>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    </main>
  </div>
</div>
