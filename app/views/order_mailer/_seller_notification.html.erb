<h2>Hello <%= order.seller&.name || "Seller" %>,</h2>

<p>
  Order ##<%= order.id %> has been paid by
  <%= [order.first_name, order.last_name].compact.join(" ") %>
  (<%= order.email %>).
</p>

<p>
  Phone: <%= order.phone_number || "N/A" %><br>
  Address: <%= order.address || "N/A" %>
</p>

<p>Please prepare the shipment and update tracking when ready.</p>

<%# Always show KES first, then USD conversion %>
<p>
  <strong>Total:</strong>
  <%= number_to_currency(order.total, unit: "KES ") %>
  <% usd_total = ExchangeRateService.convert(order.total, from: "KES", to: "USD") %>
  <span class="text-muted small">≈ <%= number_to_currency(usd_total, unit: "USD ") %></span>
</p>

<p>Thank you for using our platform.</p>
<p>Best regards,<br>The Tajaone Team</p>